# ZoneLaunch v0.1.2

> 这一版的重点是**把发版与安装体验做完整**：GitHub Release **默认英文**，顶部提供 **中文入口**；维护者可用 `pnpm release:tag` 一键打 tag 触发 CI，并在文档中用截图写清 macOS 门禁「仍要打开」的步骤。

**[English →](https://github.com/jawQ/app-timezone-launchers/blob/v0.1.2/docs/release-notes/v0.1.2-en.md)**

---

## 概览

ZoneLaunch v0.1.2 是 v0.1.1 之后的维护更新，主要面向**文档与发版链路**，不改变按时区启动应用的核心机制。

系统时区与社交软件时间不一致时，仍可通过脚本或 ZoneLaunch App 为指定应用注入 `TZ`；本版让「下载 App → 首次打开 → 发下一版」的路径更清晰。

---

## 重点内容

- **中英双语 Release 日志**：**默认英文**；顶部 **[中文 →](…-zh.md)** 进入完整中文说明。可手写 `docs/release-notes/v*-en.md` / `v*-zh.md`，或从 git 自动生成。
- **维护者一键发版**：根目录 `package.json` 提供 `pnpm release:tag`（对齐 supermarkets 的 `miniapp:tag`），自动 patch 递增、推送 tag，由 Actions 打包 ad-hoc 签名 zip。
- **门禁安装说明 + 截图**：主 README 与安装文档说明「Not Opened」与 **Privacy & Security → Open Anyway**，避免用户误点移到废纸篓。
- **项目背景文案**：补充「改系统时区导致微信 / 飞书时间与物理时区不一致」的痛点与方案说明（中英）。

---

## 变更

### Release 与 CI

- 新增 `scripts/generate-release-notes.sh`：英文优先正文 + 中文入口链接 + 下载安装说明。
- `release-macos-app.yml` 使用 `fetch-depth: 0` + `body_path` 写入 Release 描述。
- 新增 `pnpm release:notes` 便于本地预览。

### 文档

- 新增 `docs/app/releasing.md`（维护者发版全流程）。
- 模块化用户文档：`docs/scripts/*`、`docs/app/*`。
- Bundle ID 继续强绑定 `app.zonelaunch.launcher`；安装脚本清理双图标逻辑保持不变。

---

## 下载与安装

**资源**

- `ZoneLaunch-0.1.2-macos.zip` — `ZoneLaunch.app` + `README-FIRST.txt`（ad-hoc 签名，**未公证**）
- `SHA256SUMS` — 校验和

**安装步骤**

1. 解压后将 **ZoneLaunch.app** 拖到「应用程序」。
2. 首次打开常被门禁拦截：点 **Done / 完成**，再到 **系统设置 → 隐私与安全性 → Open Anyway / 仍要打开**。
3. 图文说明：[从 Release 安装](https://github.com/jawQ/app-timezone-launchers/blob/master/docs/app/install-from-release.zh-CN.md)

**更轻量：Shell 启动命令**

```bash
git clone https://github.com/jawQ/app-timezone-launchers.git
cd app-timezone-launchers
./install.sh --feishu --wechat
```

**应用身份：** Bundle ID `app.zonelaunch.launcher`
